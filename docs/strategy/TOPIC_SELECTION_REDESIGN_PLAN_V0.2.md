# 소재 선정 구조 개편 기획 v0.2

> 작성일: 2026-08-06
> 상태: 데이터 복구 완료, 블라인드 분류 대기
> 결론 제한: 이 문서는 `axis_type`, 카니발 예외, 발행 배분 비율을 아직 운영 규칙으로 확정하지 않는다.

## 한눈에 보는 결론

지금 확정할 수 있는 것은 두 가지다.

1. 과거 성과 원장은 TOP20 헤딩과 제목 표기 흔들림 때문에 크게 누락돼 있었다.
2. 누락을 복구한 뒤에도 현재 Codex와 Claude는 이미 성패를 봤기 때문에 소재 유형 분류자가 될 수 없다.

따라서 순서는 아래로 고정한다.

```text
데이터 복구
→ 성패를 가린 익명 자료 생성
→ 결과를 본 적 없는 새 분류자 2명이 독립 분류
→ 같은 발행 구간 안에서만 성패 비교
→ 2026-08-14 D14 관측 완료
→ 2026-08-15 재집계
→ 그때 운영 규칙 개정안을 별도 승인
```

## 1. 이번에 확인·수정한 데이터 기반

### 1-1. URL·통계 갱신분을 먼저 기준선으로 사용

별도 작업공간 `codex/url-stats-reconciliation-20260806`의 미커밋 변경을 원본 그대로 읽어 새 격리 브랜치에 복제했다. 원본 브랜치는 커밋·병합·수정하지 않았다.

이 기준선에는 아래가 포함된다.

- 2026-08-05 관리자 통계 1,104회, 통합검색 80.48%
- 2026-08-06 daily report와 TOP20 20행
- 공개 확인된 제목·URL·절대 발행일
- 확인되지 않은 065·066·067 URL은 대기 유지
- 폐기된 087·172 상태 유지

### 1-2. TOP20 헤딩 6종을 모두 인식

기존 파서는 `## 게시글 TOP20`만 읽었다. 번호가 붙거나 `TOP 20`처럼 띄어 쓴 헤딩도 읽도록 수정했다.

현재 실제 리포트에서 확인된 형식은 6종이다.

- `## 게시글 TOP20`
- `## 4. 게시글 TOP20`
- `## 4. 게시글 TOP 20`
- `## 3. 게시글 TOP 20`
- `## 3. 게시글 TOP 20 관찰`
- `## 5. 게시글 TOP20`

최신 2026-08-06 파일까지 전수 검산한 결과:

| 항목 | 결과 |
| --- | ---: |
| daily 파일 | 56일 |
| 인식한 헤딩 | 56/56 |
| TOP20 순위 행 | 1,020행 |
| 매핑된 관측 행 | 1,003행 |
| 미해결 관측 행 | 17행 |
| 합계 대조 | 1,003 + 17 = 1,020 |

7월 11~15일은 헤딩은 있지만 숫자 순위 행이 0개다. 이 5일은 유효 TOP20 관측으로 만들지 않는다.

### 1-3. daily 원본은 고치지 않고 등록부에 별칭을 둠

`POSTING_REGISTRY.json` 최상위에 `title_aliases`를 추가했다. 유사도 자동 매칭은 사용하지 않는다.

매칭 순서는 아래다.

1. daily의 명시적 글번호
2. 등록부 정본 제목 정확 일치
3. 등록부의 확인된 별칭 정확 일치
4. 어느 것도 아니면 `unmapped_titles`

같은 별칭이 두 글에 연결되면 한쪽을 추측하지 않고 미매칭으로 남긴다.

기존 수집기 코드에 박혀 있던 086번 제목 이력도 `title_aliases`로 옮겼다.

| 단계 | 미매칭 제목 | 미매칭 행 |
| --- | ---: | ---: |
| 새 헤딩 파서, 별칭 이전 전 | 22 | 85 |
| 확인 별칭 적용 후 | 9 | 17 |
| 회수 | 13 | 68 |

남은 9개는 등록 글번호를 확정할 직접 근거가 부족해 보존했다.

- 베이지 중문 과거 제목 2종: 2행
- 멤브레인도어/ABS도어 제목: 6행
- 문장군 현관중문 과거 제목: 1행
- 방문·화장실문 과거 제목 3종: 6행
- 중문 컬러 과거 제목: 1행
- 워너청담 비핵심 제목: 1행

이 17행을 임의로 기존 글에 붙이면 성패가 다시 바뀔 수 있으므로 현재는 미해결이 정답이다.

## 2. 복구 뒤 성과 원장 상태

| verdict | 글 수 |
| --- | ---: |
| landed | 38 |
| faded | 85 |
| unobserved | 55 |
| pending | 3 |
| 합계 | 181 |

이 숫자는 소재 처방이 아니다. 데이터가 어느 정도 복구됐는지를 보여주는 현황이다.

특히 아래 주장은 아직 확정하지 않는다.

- 새 축이 파생 소재보다 성공한다.
- 7월 하순 하락은 포화가 아닌 소재 탓이다.
- 큰 키워드는 기존 글이 있어도 정면 재공략해야 한다.
- 카니발 예외 기준은 평균 6위 또는 검색량 2,000이어야 한다.
- 주간 배분은 새 축·정면 공략 70%, 파생 30%여야 한다.

## 3. 블라인드 분류 설계

### 3-1. 분류자 자격

분류자는 `post_performance.json`의 verdict, 과거 landed/faded 목록, 이 기획서 v0.1의 결론을 본 적 없는 새 세션이어야 한다.

- 현재 Claude: 제외
- 현재 Codex: 제외
- 새 분류자 A: 필요
- 새 분류자 B: 필요
- 두 분류자가 충돌한 항목만 새 판정자 C 또는 `unresolved`

### 3-2. 분류 자료

`npm run topic:blind:prepare`가 아래 파일을 만든다.

`outputs/reports/topic_analysis/2026-08-06_topic_blind_dataset.json`

123개 초기 판정 완료 글을 담되 각 행에는 아래 다섯 필드만 있다.

- `blind_id`
- `title`
- `target_keywords`
- `topic_summary`
- `has_summary`

글번호, 파일명, URL, 발행일, 발행순서, queue_id, verdict, 관측일, 순위, 조회수는 넣지 않는다.

제목·키워드·요약의 날짜 표기는 `[날짜 마스킹]`으로 바꾼다. 결과·운영 방식을 암시하는 문장은 문장 단위로 제거하고, 등록부의 실제 글번호나 `NNN_이름.md` 형태의 파일명이 다른 게시글을 가리키는 문맥에서는 `[다른 글]`로 바꾼다. 생성기는 정제 후 금지 패턴과 깨진 마스킹 토큰을 다시 검사하며 누출이 하나라도 있으면 파일을 쓰지 않는다. `has_summary`는 키워드나 정제된 요약 중 하나라도 있으면 `true`이며, 제목만 있는 27건을 분석에서 분리하기 위한 값이다. 운영 메모를 제거한 자리에 새 요약을 작성해 채우거나 요약 길이를 맞추기 위해 소재 설명을 절단하지 않는다. 70자 일괄 절단은 요약이 있는 96건 중 62건의 분류 근거를 줄이면서도 제목·키워드의 시기 지문을 없애지 못하므로 적용하지 않는다.

익명 ID는 SHA-256 앞 12자리를 숫자가 없는 `a~p` 문자 코드로 표현한다. 동일 글은 재실행할 때 같은 ID가 나오므로 언블라인딩은 가능하지만, 저장소에 접근하면 글번호 후보를 대입해 역산할 수 있다. 따라서 분류자에게는 저장소가 아니라 `C:\Users\hjh\블라인드분류`의 데이터셋과 라벨 정의 두 파일만 전달한다.

### 3-3. 먼저 분류할 특징

`new_axis`/`derivative`는 과거 순서와 기존 글 관계를 알아야 하므로 첫 블라인드 분류의 정답처럼 사용하지 않는다. 먼저 제목과 소재만 보고 판정 가능한 특징을 분류한다.

| 필드 | 허용 값 |
| --- | --- |
| `intent_type` | cost / symptom_repair / comparison / installation_constraint / design_case / brand_service / other |
| `demand_breadth` | broad / mid / narrow |
| `problem_explicitness` | explicit / implicit / none |
| `service_alignment` | direct / adjacent / unclear |
| `confidence` | high / medium / low |

분류자는 성과를 예상하거나 `잘될 글`, `실패할 글`을 적지 않는다.

### 3-4. 독립성 검사

두 분류 결과를 합치기 전에 아래를 검사한다.

- 두 파일 모두 모든 `blind_id`를 정확히 한 번 포함
- 추가 ID와 누락 ID가 없음
- 허용 값 밖의 라벨이 없음
- 분류자가 결과 데이터를 보지 않았다는 선언이 있음
- 일치율과 Cohen's kappa를 필드별 기록

일치하지 않은 항목을 현재 Claude나 Codex가 임의로 정리하지 않는다.

## 4. 포화를 통제하는 비교 방법

전체 기간의 성공률을 앞뒤로 단순 비교하지 않는다. 같은 시기에 발행된 글끼리 비교한다.

| 구간 | 용도 |
| --- | --- |
| 2026-06-01~06-15 | 동일 시점 내부 비교 A |
| 2026-06-16~06-30 | 동일 시점 내부 비교 B |
| 2026-07-01~07-10 | 동일 시점 내부 비교 C |
| 2026-07-11~07-18 | 동일 시점 내부 비교 D |
| 2026-07-19~07-31 | 8월 14일까지 잠정, 8월 15일 재계산 E |

2026-08-06 성과 원장 기준의 단순 현황은 아래와 같다. 소재 특징과 결합하기 전 수치이며 원인 해석에는 쓰지 않는다.

| 구간 | 판정 글 | landed | faded | 달력상 D14 경과 | 달력상 D14 미경과 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A | 18 | 14 | 4 | 18 | 0 |
| B | 50 | 16 | 34 | 50 | 0 |
| C | 28 | 7 | 21 | 28 | 0 |
| D | 13 | 1 | 12 | 13 | 0 |
| E | 13 | 0 | 13 | 5 | 8 |

따라서 E 구간의 `0/13`은 현재 원장 출력일 뿐 최종 성공률이 아니다. 남은 8개가 달력상 D14를 지나고 관측 커버리지를 확인하기 전까지 잠정값으로만 쓴다.

각 구간 안에서 라벨별로 아래를 낸다.

- eligible 글 수
- landed 수와 비율
- faded 수와 비율
- 반대 라벨 대비 성공률 차이
- 표본이 작으면 신뢰구간과 Fisher exact test

한 구간에서 우연히 나온 차이를 전체 규칙으로 승격하지 않는다. 최소 3개 비교 가능 구간에서 방향이 반복되는지 본다. 7월 19~31일 구간은 전부 D14가 끝나는 2026-08-14 전에는 `0/13 확정`이라고 쓰지 않는다.

이 설계는 시점별 포화 수준 차이를 줄이지만, 네이버 알고리즘·경쟁 콘텐츠·제목 품질 같은 외부 요인을 모두 제거하지는 못한다. 결과는 인과 확정이 아니라 운영 실험 후보를 고르는 근거다.

블라인드 입력에서도 제목의 `3가지`·물음표 사용, 제목 길이, `target_keywords` 기록 개수처럼 콘텐츠 자체 또는 분류 근거인 시기 지문은 제거하지 않는다. 따라서 블라인드는 운영 메타데이터와 성과 직접 단서를 차단할 뿐 발행 시기를 완벽히 숨기지는 못한다. 소재 라벨과 성과의 관계가 실제로는 시기별 작성 방식 차이에서 나온 교란일 수 있으므로, 언블라인딩 후에는 반드시 위 발행 구간 내부에서만 비교하고 전체 기간 상관을 원인으로 해석하지 않는다.

## 5. 2026-08-15 의사결정 게이트

8월 15일에 아래 순서로 다시 실행한다.

1. 8월 14일 관리자 TOP20까지 수집 완료 확인
2. `npm run performance:collect`
3. 7월 19~31일 발행분 D14 완주 여부 확인
4. 확정된 독립 분류 라벨과 성과 원장을 결합
5. 같은 발행 구간 내부 비교표 생성
6. 표본 수, 효과 크기, 구간 반복성을 함께 검토
7. 그 결과로만 운영 규칙 개정안 작성

그 전에는 아래 파일을 바꾸지 않는다.

- `AGENTS.md`의 글감 선정 계약
- `TOPIC_SELECTION_SCORECARD.md` 배점
- 카니발 차단·예외 기준
- 큐의 `axis_type` 또는 70/30 배분 규칙
- 발행량 기준

## 6. 운영자용 쉬운 해석

지금은 “무슨 소재를 써야 한다”를 알아낸 단계가 아니다.

정확히는 아래까지 왔다.

```text
성적표에 빠진 시험지를 대부분 회수했다.
오타로 다른 학생처럼 잡힌 답안도 확인된 것만 합쳤다.
누가 합격했는지는 이미 본 사람을 채점자에서 제외했다.
새 채점자에게 이름과 점수를 가린 시험지만 줄 준비를 끝냈다.
같은 시험일의 학생끼리 비교한 뒤 규칙을 만들 것이다.
```

따라서 다음 글감은 v0.1의 `새 축 우선`, `정면 대결 허용`만 믿고 뽑지 않는다. 기존 AGENTS.md와 scorecard를 그대로 적용하되, 새 전략 규칙은 8월 15일 검증 뒤 별도 승인한다.

## 7. 재현 명령

```powershell
npm run test:performance
npm run performance:collect
npm run topic:blind:prepare
npm run test:topic-blind
npm run render:strategy:check
npm run ops:daily
npm run validate
git diff --check
```
