# 발행 전 체크리스트

> 목적: 네이버 블로그에 붙여넣기 전에 발행 사고를 막는다.  
> 적용 대상: 신규 글, 리라이팅 발행본, 중요 보강 글

---

## 1. 파일 구조

- [ ] `posts/` 파일은 발행 본문만 담고 있다.
- [ ] 파일 상단에 `## 제목 후보 5개` 섹션이 있으면, 그 아래 첫 `# 제목`이 실제 발행 제목으로 명확하다.
- [ ] `# 제목 후보`처럼 옛 제작노트 형식의 1단 제목이 남아 있지 않다.
- [ ] `검색 의도 분석`, `썸네일 카피`, `품질 채점 결과`, `예상 성능`이 본문 파일에 남아 있지 않다.
- [ ] 제작노트 파일을 만들지 않았다. 문장군 블로그는 `posts/NNN_키워드.md` 단일 발행 MD만 사용한다.
- [ ] 자동 검수 결과는 `outputs/checks/NNN_키워드_check.md`에 저장돼 있다.

## 2. 제목

- [ ] 최종 제목 1개가 명확하다.
- [ ] 제목 후보 5개가 있다.
- [ ] 제목 후보가 검색형, 현장형, 통념반박형, 손실회피형, 서비스/브랜드형 중 최소 3가지 이상을 섞고 있다.
- [ ] `3가지`형 제목이 후보 5개 전체를 차지하지 않는다.
- [ ] 질문형 제목이라면 핵심 키워드와 고객 불안/의심이 함께 살아 있다. 단순 궁금증형·낚시형 질문이 아니다.
- [ ] 타겟 키워드가 앞쪽에 있다.
- [ ] `중문` 단독 제목이 아니다.
- [ ] 최근 5편과 같은 제목 패턴이 과도하게 반복되지 않는다.

## 3. 본문 품질

- [ ] 검색자의 실제 고민이 도입부에 드러난다.
- [ ] 첫 5문단 안에 고객의 손실, 불안, 선택 갈등 중 하나가 보인다.
- [ ] 현장 기준, 사례, 비교표 중 최소 1개 이상이 있다.
- [ ] `실제 시공 현장` 단락에 구조 변수 3개 이상이 자연스럽게 들어간다.
- [ ] 098번 이후 통계 기반 신규 글은 공백 제외 본문 1,500~2,500자를 합격 범위로 보되, 분량보다 현장 상담식 전개와 고객 문제 해결이 우선이다.
- [ ] 통계 기반 신규 글은 AppSheet 후매칭용 현장 조건과 사진 방향이 본문 문장 안에서 자연스럽게 드러난다.
- [ ] AppSheet 후매칭 문장이 `[사진:]`, `[현장 검색:]` 같은 내부 작업 지시처럼 보이지 않는다.
- [ ] `humanize-korean` 기준으로 AI 티를 점검했으며, 윤문 과정에서 사실·수치·제품명·지역명·현장 조건을 바꾸지 않았다.
- [ ] 실제 고객/현장 사례처럼 쓴 문장은 `EVIDENCE.json`의 `evidence_ref`와 연결돼 있다.
- [ ] AppSheet 근거가 없는 경우 실제 사례처럼 꾸미지 않고 일반 현장 조건으로 표현했다.
- [ ] 문장군 제품명과 시공 가능 범위가 정확하다.
- [ ] 근거 없는 고정 가격이나 과장 수치를 쓰지 않았다.
- [ ] CTA가 광고 문구가 아니라 문제 해결 흐름 뒤에 자연스럽게 이어진다.
- [ ] `[사진:` 플레이스홀더가 발행 본문에 남아 있지 않다.
- [ ] `[이미지:]`, `[AppSheet 확인]`, `[제작자 메모]`, `## 운영 메모` 같은 내부 지시문이 없다.
- [ ] 사진 삽입 방향은 별도 지시가 아니라 본문 문장 흐름으로 유도된다.

## 4. 금지 표현

- [ ] `물론`, `또한`, `더불어`, `이처럼`, `결론적으로`를 쓰지 않았다.
- [ ] `안녕하세요 문장군입니다`로 시작하지 않는다.
- [ ] `보양`, `현관문`, `무조건`, `완벽 방음`, `완전 방음`이 없다.
- [ ] 불가 지역을 가능 지역처럼 쓰지 않았다.

## 4-1. 중앙 브랜드 claim gate

- [ ] 리뷰 수는 `네이버 상품 리뷰 3.5만 개 이상`처럼 범위를 한정했다. `15,000개`, `4,000개`, `전체 5만` 표현은 쓰지 않았다.
- [ ] 중문 일정은 중앙 `EVIDENCE_REGISTER.md` 기준 `보통 3~6일` 범위로 설명했다. 기존 `3~4일` 표현은 쓰지 않았다.
- [ ] A/S는 `도어 3년, 중문 2년 보증` 범위로만 썼고 `무조건 무상`, `평생 A/S`처럼 쓰지 않았다.
- [ ] 가격, 월 납입, 무이자 할부, 이벤트, 배송/반품, 패키지 구성은 중앙 `EVIDENCE_REGISTER.md`에서 publishable/vetted 상태인지 확인했다.
- [ ] 중앙 상품 위키가 `vetted`여도 가격, 리뷰 수, A/S, 일정, 이벤트, 옵션 운영 여부는 `EVIDENCE_REGISTER.md`와 `OPEN_QUESTIONS_REGISTER.md`를 다시 확인했다.

## 5. 링크와 CTA

- [ ] 실제 네이버 내부링크가 최소 2개 이상 있다.
- [ ] `현재 발행 예정`, `기존 000번`, `등록된 000번` 같은 임시 문구가 없다.
- [ ] 무료 방문실측 또는 네이버 예약 CTA가 자연스럽게 들어가 있다.
- [ ] 광고처럼 강한 권유가 아니라 편한 예약 안내 톤이다.

## 6. 해시태그

- [ ] 해시태그는 5~10개다.
- [ ] 해시태그에 띄어쓰기가 없다.
- [ ] `#문장군`, `#문장군중문`이 포함돼 있다.
- [ ] 본문 중간이 아니라 태그란 입력용 섹션으로 분리돼 있다.

## 7. 명령어 검수

발행 전 아래 명령을 실행한다.

```bash
npm run validate:posts
```

중요 글이나 리라이팅 발행본은 아래 명령까지 확인한다.

```bash
npm run validate:posts:strict
```

098번 이후 신규 글에서 본문 길이 기준을 벗어나면 WARN으로 보고 보강 여부를 판단한다.
경고가 남아도 발행할 수는 있지만, 경고 사유를 알고 발행해야 한다.
